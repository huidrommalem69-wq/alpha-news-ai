{
  "version": 2,
  "builds": [
    { "src": "vite.config.*", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ],
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
}